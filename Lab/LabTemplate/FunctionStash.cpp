// So this is where all the reusable functions are hiding?

#include "FunctionStash.h"

void printVector(vector<int> &vectorName) {
    for (int i = 0; i < vectorName.size(), i++) {
        cout << vectorName[i] << endl;
    }
}